# Push the file in git command

```bash
> git add .  # add all file
> git commit -m "give some massage"  # commit the file
> git push origin main    # push in the local to remote repository means github

```